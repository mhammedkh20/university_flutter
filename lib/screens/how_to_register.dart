import 'package:flutter/material.dart';

class HowToRegister extends StatelessWidget {
  const HowToRegister({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}